# The embedding in RAG

Different embedding model contains different vector space, token limitation, training domain.
So, in ingestion and retrieval stage must use the exact same model, also in evaluation.

## Sparse Embedding Model

- BM25(TF-IDF)
- Bert based model, it can extend words depending on model power

## Cosine Similarity Application



